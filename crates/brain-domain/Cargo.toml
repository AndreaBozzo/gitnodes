[package]
name = "brain-domain"
version = "0.1.0"
edition = "2024"
publish = false
description = "Pure domain types and errors for brain_ui. No leptos, no axum, no I/O."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
thiserror = "2"

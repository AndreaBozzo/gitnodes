# Brain UI — Environment Variables
# Copy to .env and fill in your values.

# GitHub OAuth App credentials.
# Register one at: https://github.com/organizations/<your-org>/settings/applications
GITHUB_CLIENT_ID=your_client_id_here
GITHUB_CLIENT_SECRET=your_client_secret_here

# Target repository the app reads from and writes to.
# TARGET_GITHUB_ORG is also the org required for login (membership check).
TARGET_GITHUB_ORG=Dritara-Digital
TARGET_GITHUB_REPO=Brain
TARGET_GITHUB_BRANCH=main

# User-facing branding. `BRAND_NAME` appears in the header and page title;
# `BRAND_ORG_LABEL` appears in access-denied copy (typically matches the org).
BRAND_NAME=Dritara Brain
BRAND_ORG_LABEL=Dritara-Digital

# Server-side GitHub credentials, used by the webhook handler when no user
# session is present. Two options — pick one:
#
#   A. GitHub App (recommended). Install the App on the target org/repo with
#      Contents: Read & write, Pull requests: Read & write, Issues: Read &
#      write, Metadata: Read. Tokens auto-rotate hourly.
#GITHUB_APP_ID=
#GITHUB_APP_INSTALLATION_ID=
# Inline the PEM as a single-line string with literal `\n` between lines:
#GITHUB_APP_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\nMIIEpAIBAAKCAQEA...\n-----END RSA PRIVATE KEY-----\n"
# …or point at a file on disk (preferred for k8s-style secret mounts):
#GITHUB_APP_PRIVATE_KEY_PATH=/run/secrets/github-app.pem
#
#   B. Fine-grained PAT fallback. Used when GITHUB_APP_* is unset, or when the
#      App-token mint fails. Must have the same repo permissions as above.
#GITHUB_TOKEN=
